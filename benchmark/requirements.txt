# FieldSight ASR Benchmark — Python dependencies
# Run: pip install -r requirements.txt   (also needs system ffmpeg/ffprobe)

streamlit>=1.40
requests>=2.31
boto3>=1.34
cartesia>=3.2            # Cartesia Ink-2 streaming STT (WebSocket)
dashscope>=1.20          # Qwen3-ASR-Flash + Fun-ASR (Alibaba DashScope)
anthropic>=0.40          # LLM-as-judge (reference-free scoring)
jiwer>=3.0               # WER / CER metrics
pandas>=2.0
python-dotenv>=1.0
