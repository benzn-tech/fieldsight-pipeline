psycopg[binary]>=3.1
# NOTE: pgvector deliberately EXCLUDED — it hard-requires numpy, which SAM's
# PythonPipBuilder fails to resolve for py3.11. The migrate Lambda only runs
# SQL text (no vector binding); connection.py treats pgvector as optional.
# Phase 3/4 functions that bind vectors revisit this (own layer or container).
